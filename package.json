{
  "name": "fresh-framework-helper",
  "displayName": "Fresh framework helper",
  "description": "Deno Fresh Framework Snippets & Code generator",
  "repository": "https://github.com/hashrock/fresh-mini-codegen",
  "license": "MIT",
  "version": "0.0.6",
  "publisher": "hashrock",
  "icon": "logo.png",
  "engines": {
    "vscode": "^1.81.0"
  },
  "categories": [
    "Other",
    "Snippets"
  ],
  "keywords": [
    "deno",
    "fresh"
  ],
  "activationEvents": [
    "workspaceContains:fresh.gen.ts"
  ],
  "contributes": {
    "views": {
      "hashrock-fresh-helper": [
        {
          "type": "webview",
          "id": "hashrock.fresh.routeView",
          "name": "Fresh Routes",
          "when": "is-fresh-project"
        },
        {
          "type": "webview",
          "id": "hashrock.deno.kvView",
          "name": "KV Viewer"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "hashrock-fresh-helper",
          "title": "Fresh Helper",
          "icon": "media/deno-sidebar.svg"
        }
      ]
    },
    "commands": [
      {
        "command": "hashrock.fresh.generateRoute",
        "title": "New Route Component",
        "when": "is-fresh-project"
      },
      {
        "command": "hashrock.fresh.generateLayout",
        "title": "New Layout",
        "when": "is-fresh-project"
      },
      {
        "command": "hashrock.fresh.generateComponent",
        "title": "New Component",
        "when": "is-fresh-project"
      },
      {
        "command": "hashrock.fresh.generateIsland",
        "title": "New Island Component",
        "when": "is-fresh-project"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "when": "is-fresh-project && explorerResourceIsFolder && resourceFilename =~ /routes/",
          "command": "hashrock.fresh.generateRoute",
          "group": "Fresh"
        },
        {
          "when": "is-fresh-project && explorerResourceIsFolder && resourceDirname =~ /routes/",
          "command": "hashrock.fresh.generateRoute",
          "group": "Fresh"
        },
        {
          "when": "is-fresh-project && explorerResourceIsFolder && resourceFilename =~ /islands/",
          "command": "hashrock.fresh.generateIsland",
          "group": "Fresh"
        },
        {
          "when": "is-fresh-project && explorerResourceIsFolder && resourceDirname =~ /islands/",
          "command": "hashrock.fresh.generateIsland",
          "group": "Fresh"
        },
        {
          "when": "is-fresh-project && explorerResourceIsFolder && resourceFilename =~ /components/",
          "command": "hashrock.fresh.generateComponent",
          "group": "Fresh"
        },
        {
          "when": "is-fresh-project && explorerResourceIsFolder && resourceDirname =~ /components/",
          "command": "hashrock.fresh.generateComponent",
          "group": "Fresh"
        },
        {
          "when": "is-fresh-project && explorerResourceIsFolder && resourceFilename =~ /routes/",
          "command": "hashrock.fresh.generateLayout",
          "group": "Fresh"
        },
        {
          "when": "is-fresh-project && explorerResourceIsFolder && resourceDirname =~ /routes/",
          "command": "hashrock.fresh.generateLayout",
          "group": "Fresh"
        }
      ]
    }
  },
  "main": "./out/extension.js",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "changelog": "npx standard-version",
    "kv:build": "esbuild kv/main.jsx --bundle --outfile=media/kv/main.js --minify"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@types/node-fetch": "^2.6.6",
    "@types/vscode": "^1.81.0",
    "@typescript-eslint/eslint-plugin": "^6.4.1",
    "@typescript-eslint/parser": "^6.4.1",
    "@vscode/test-electron": "^2.3.4",
    "classnames": "^2.3.2",
    "esbuild": "0.19.4",
    "eslint": "^8.47.0",
    "glob": "^10.3.3",
    "mocha": "^10.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
